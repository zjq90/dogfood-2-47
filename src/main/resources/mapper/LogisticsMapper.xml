<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mall.mapper.LogisticsMapper">
    
    <resultMap id="BaseResultMap" type="com.mall.entity.Logistics">
        <id column="id" property="id"/>
        <result column="order_id" property="orderId"/>
        <result column="logistics_no" property="logisticsNo"/>
        <result column="company" property="company"/>
        <result column="status" property="status"/>
        <result column="current_location" property="currentLocation"/>
        <result column="details" property="details"/>
        <result column="update_time" property="updateTime"/>
        <result column="create_time" property="createTime"/>
        <result column="order_no" property="orderNo"/>
    </resultMap>
    
    <sql id="Base_Column_List">
        l.id, l.order_id, l.logistics_no, l.company, l.status, l.current_location, l.details, l.update_time, l.create_time
    </sql>
    
    <select id="selectById" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>, o.order_no
        FROM logistics l
        LEFT JOIN `order` o ON l.order_id = o.id
        WHERE l.id = #{id}
    </select>
    
    <select id="selectByOrderId" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>, o.order_no
        FROM logistics l
        LEFT JOIN `order` o ON l.order_id = o.id
        WHERE l.order_id = #{orderId}
    </select>
    
    <select id="selectList" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>, o.order_no
        FROM logistics l
        LEFT JOIN `order` o ON l.order_id = o.id
        <where>
            <if test="orderNo != null and orderNo != ''">
                AND o.order_no LIKE CONCAT('%', #{orderNo}, '%')
            </if>
            <if test="logisticsNo != null and logisticsNo != ''">
                AND l.logistics_no LIKE CONCAT('%', #{logisticsNo}, '%')
            </if>
        </where>
        ORDER BY l.create_time DESC
    </select>
    
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO logistics (order_id, logistics_no, company, status, current_location, details, update_time)
        VALUES (#{orderId}, #{logisticsNo}, #{company}, #{status}, #{currentLocation}, #{details}, #{updateTime})
    </insert>
    
    <update id="update">
        UPDATE logistics
        <set>
            <if test="logisticsNo != null">logistics_no = #{logisticsNo},</if>
            <if test="company != null">company = #{company},</if>
            <if test="status != null">status = #{status},</if>
            <if test="currentLocation != null">current_location = #{currentLocation},</if>
            <if test="details != null">details = #{details},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </set>
        WHERE id = #{id}
    </update>
    
    <update id="updateByOrderId">
        UPDATE logistics
        <set>
            <if test="logisticsNo != null">logistics_no = #{logisticsNo},</if>
            <if test="company != null">company = #{company},</if>
            <if test="status != null">status = #{status},</if>
            <if test="currentLocation != null">current_location = #{currentLocation},</if>
            <if test="details != null">details = #{details},</if>
            update_time = NOW()
        </set>
        WHERE order_id = #{orderId}
    </update>
    
    <delete id="deleteById">
        DELETE FROM logistics WHERE id = #{id}
    </delete>
</mapper>
